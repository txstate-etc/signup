Good news, <%= @reservation.user.name %>!

You were on the waiting list for "<%= @reservation.session.topic.name %>",
and space has become available! You have automatically been enrolled in the
class, and need take no additional action to attend.

If you won't be able to make it to this session after all, you can cancel
your reservation from the Training Reservation System:

<%= url_for :controller => "reservations", :only_path => false %>

----

Your Reservation

What: <%= @reservation.session.topic.name %>
<% if @reservation.session.multiple_occurrences? -%>
When: This session meets <%= @reservation.session.occurrences.count %> times:
<% @reservation.session.occurrences.each do |o| -%>
  <%= formatted_time_range(o.time, @reservation.session.topic.minutes) %>
<% end -%>
<% else -%>
When: <%= formatted_time_range(@reservation.session.time, @reservation.session.topic.minutes) %>
<% end -%>
Where: <%= @reservation.session.loc_with_site %>
Who:  <%= @reservation.user.name %>
Description: <%= auto_link( @reservation.session.topic.description ) %>

----
<% if @reservation.session.topic.documents.present? -%>
Download class materials:
<% @reservation.session.topic.documents.each do |d| -%>
<%= d.friendly_name + ' : ' + ReservationMailer.absolute_url(d.item.url) %>
<% end -%>
----
<% end -%>

You can use the attached ICS file to add the session to your calendar software.

