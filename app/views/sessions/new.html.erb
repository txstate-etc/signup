<%= error_messages_for 'session' %>

<% form_for( [ @session.topic, @session ] ) do |f| %>
	<%= render :partial => "form_fields", :locals => { :f => f }%>
	
	<br/><br/>
	
	<%= submit_tag "Create Session"%>
<% end %>